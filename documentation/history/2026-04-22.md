# 2026-04-22

## Done

- Added a live "Git status" row to the settings tab (`src/app/settings/settings-tab.ts`) that tells the user why git integration may not be producing snapshots even when enabled.
    - Mobile → "Git integration is desktop-only and is unavailable on this platform."
    - Desktop, git not installed or vault not inside a repo → "Git not detected for this vault. Ensure git is installed and the vault is inside a git repository to see git snapshots."
    - Desktop, git available → "Git repository detected. Git snapshots will appear on the timeline."
- Uses `Platform.isDesktopApp` short-circuit before invoking `GitService.isAvailable` to avoid a pointless async check on mobile.
- Updated `documentation/Business Rules.md` "Git Integration" section to reflect the new status indicator.

## Notes

- No behavior change to snapshot fetching — `SnapshotService.getGitSnapshots` already failed silently when git was unavailable; this is purely a UX affordance so users understand the state.
- UI change: requires manual verification in Obsidian. To verify: open **Settings → Time Machine**, confirm the "Git status" row shows the expected message on (a) a vault inside a git repo, (b) a vault outside any git repo, (c) mobile.

## Verification

- `bun run tsc` — clean
- `bun run lint` — clean (--max-warnings 0)
- `bun test` — 104 pass, 0 fail
- `bun run build` — succeeded
